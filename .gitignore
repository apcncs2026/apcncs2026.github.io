node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Office temp/lock files
~$*

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude AI files
CLAUDE.md
.claude/

# Profile images - ignore everything except .webp files
static/images/profile/organizer/*
!static/images/profile/organizer/*.webp
static/images/profile/speaker/*
!static/images/profile/speaker/*.webp

# Sponsor images - keep all image files
# No exclusions needed as we want to track sponsor logos
static/images/sponsor/*
!static/images/sponsor/*.webp

# Conference photo originals (multi-GB DSLR/phone files from the SPMS Comms team).
# Only the web-optimized WebP versions under static/gallery/ are committed; the
# raw originals are kept out of git (they exceed the GitHub Pages 1 GB site limit).
# Regenerate static/gallery/ from originals with: python scripts/process_gallery.py
static/photos/

design/